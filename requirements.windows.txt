fastapi>=0.110.0
uvicorn>=0.29.0
python-dotenv>=1.0.1
pydantic>=2.6.4
numpy>=1.26.4
openai>=1.14.0
faster-whisper>=1.0.1
httpx>=0.27.0
pytest>=9.0.0

# Tooling
ruff
black
mypy

# Shared logging + distributed tracing (local checkout; see shared-logging/README.md)
-e ../shared-logging

# Project contracts (single source of truth for what microservices exchange), bundled in this
# folder so the service builds on its own. Refresh with contracts/scripts/bundle.py
./vendor/contracts_microservice-0.6.0-py3-none-any.whl
